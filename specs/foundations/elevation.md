# Elevation

Use `--shadow-low` for hoverable media, `--shadow-raised` for floating controls, `--shadow-floating` for modal media, and `--shadow-soft` for quiet image depth. Use `--filter-shadow-project-image` only for transparent project artwork.

Elevation indicates interaction or stacking. Do not add a shadow for decoration when a border or spacing change communicates the hierarchy.
